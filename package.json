{
  "name": "prog-bar-test",
  "version": "0.2.0",
  "private": true,
  "homepage": "./",
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "^2.0.5"
  },
  "scripts": {
    "start": "HOST=0.0.0.0 react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "storybook": "HOST=0.0.0.0 start-storybook -p 9009 -s public",
    "build-storybook": "build-storybook -s public",
    "clean": "rm -rf build storybook-static",
    "pack": "rm -rf build storybook-static node_modules",
    "unpack": "npm install -d"
  },
  "devDependencies": {
    "@storybook/addon-actions": "^3.3.12",
    "@storybook/addon-links": "^6.1.18",
    "@storybook/addons": "^3.3.12",
    "@storybook/react": "^4.0.2",
    "babel-core": "^6.26.0"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
